## **Домашнє завдання: «Редактор оцінок»**

### **Мета**:

навчитися працювати з покажчиками, передавати змінні у функції за адресою, оперувати з масивами через покажчики.

---

### **Сюжет:**

У вас є список оцінок студента за 5 предметів. Потрібно реалізувати маленький "редактор оцінок", який дозволяє:

* виводити оцінки,
* редагувати будь-яку оцінку,
* знаходити середнє арифметичне,
* підвищити всі оцінки, нижчі за 4, на 1 бал (але не вище 5).

---

### **Що потрібно реалізувати:**

1. Оголосіть масив з 5 цілих чисел — оцінок (від 2 до 5).
2. Реалізуйте функції:

```cpp
void printGrades(int* grades, int size);
// Виводить усі оцінки

void updateGrade(int* grades, int index, int newGrade);
// Змінює оцінку за вказаним індексом

float calculateAverage(int* grades, int size);
// Повертає середнє арифметичне

void boostGrades(int* grades, int size);
// Збільшує всі оцінки нижчі за 4 на 1 (макс. 5)
```

3. У `main()` протестуйте всі функції — створіть меню з вибором дії користувача.

---

### 📌 **Обмеження:**

* **Не використовувати `new`, `delete`.**
* Всі функції повинні працювати **через покажчики**, а не через звичайні індекси.
* Масив оголошується **у `main()`**, і передається до функцій **за допомогою покажчика**.

---

🔁 **Додаткове завдання на +1 бал:**
Додати функцію `void resetGrades(int* grades, int size)`, яка ставить усі оцінки на 3.
